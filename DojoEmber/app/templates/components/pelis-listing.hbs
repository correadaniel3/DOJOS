<article class="listing">
    <img src="{{pelicula.image}}" alt="">
  <h3>{{pelicula.name}}</h3>
  <div class="detail type">
    <span>Tipo:</span> {{pelicula.type}}
  </div>
  <div class="detail city">
    <span>Ciudad:</span> {{pelicula.city}}
  </div>
</article>

{{yield}}
